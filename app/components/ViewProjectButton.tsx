'use client'
import React from 'react'

const ViewProjectButton = () => {
  return (
    <div>
        <button onClick={() => console.log('Clicked')}>View Project</button>
    </div>
  )
}

export default ViewProjectButton